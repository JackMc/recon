<h2>Probes</h2>
<ul>
  <% @scan.http_probes.each do |probe| %>
    <li><%= link_to probe.domain.fqdn, target_domain_url(@target, probe.domain) %> status: <%= link_to probe.failed ? "DOWN" : "UP", target_http_probe_url(@target, probe) %></li>
  <% end %>
</ul>
